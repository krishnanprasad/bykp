import { CommaToArrayPipe } from './comma-to-array.pipe';

describe('CommaToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
